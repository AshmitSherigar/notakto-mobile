import { Text, View } from "react-native";

export default function vsPlayer() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}>
      <Text>vsPlayer screen</Text>
    </View>
  );
}
